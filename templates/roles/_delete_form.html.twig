<form method="post" action="{{ path('app_roles_delete', {'id': role.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ role.id) }}">
    <style>
#del {
    background: #900000ff;
    color: #fafafaff;
    border: none;
    padding: 10px 20px;
    border-radius: 5px;
    cursor: pointer;
}
#del:hover {
    background: rgba(255, 0, 0, 1);
}
</style>
    <button class="btn" id="del">Delete</button>
</form>
